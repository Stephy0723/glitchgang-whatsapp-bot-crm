module.exports = {
  async execute({ reply }) {
    await reply(
      [
        "🏅 *RANKING GLITCHGANG*",
        "",
        "1. NEOXYS",
        "2. Eclipse",
        "3. Team Nova",
        "",
        "_Ranking demo. Conecta la API real para usar datos reales._"
      ].join("\n")
    );
  }
};
